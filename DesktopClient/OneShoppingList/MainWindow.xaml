<Window x:Class="OneShoppingList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:con="http://schemas.kent.boogaart.com/converters"
        mc:Ignorable="d" Title="One Shopping List"
        DataContext="{Binding Main, Source={StaticResource Locator}}" Height="461" Width="804" ResizeMode="CanResize" ShowInTaskbar="True" WindowStartupLocation="CenterScreen" WindowState="Normal" WindowStyle="SingleBorderWindow">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="6" x:Name="LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="SHOPPING LIST" FontWeight="Bold" Grid.Row="1"/>
        <ListBox Margin="0,6,0,0" BorderThickness="0" 
                 HorizontalAlignment="Stretch" Background="Transparent" 
                 Grid.Column="0" Grid.Row="2"
                 ItemsSource="{Binding ShoppingList}" 
                 SelectedItem="{Binding CurrentShoppingItem}" 
                 SelectedIndex="0">
            <ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Margin="6,0,0,0" Text="{Binding Name,  Converter={con:CaseConverter TargetCasing=Upper}}" FontSize="15"/>
                                <Border Height="1" BorderThickness="1" BorderBrush="Black" Margin="0,0,6,6"/>
                            </StackPanel>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListBox.GroupStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Width="200" Margin="0,0,3,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Column="0" TextBlock.FontSize="15" Text="{Binding caption}"/>
                        <TextBlock Grid.Column="1" TextBlock.FontSize="15" HorizontalAlignment="Right" Text="{Binding default_quantity}"/>
                        <TextBlock Grid.Column="0" Grid.Row="1" Text="{Binding category}"/>
                        <TextBlock Grid.Column="1" Grid.Row="1"  HorizontalAlignment="Right" Text="{Binding unit_size}"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBox x:Name="searchBox" Grid.Column="1" Text="{Binding SearchString, Mode=TwoWay}" TextChanged="searchBox_TextChanged"  TextBlock.FontSize="15" />
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1">
            <TextBlock Text="{Binding ShoppingList.Count}" FontWeight="Bold"/>
            <TextBlock Text=" MATCHES" FontWeight="Bold"/>
        </StackPanel>
        <TextBlock Grid.Row="1" Margin="6,0,0,0" Grid.Column="2"  Text="PRODUCT DETAILS" FontWeight="Bold"/>
        <ListBox Margin="0,6,0,0" BorderThickness="0" 
                 HorizontalAlignment="Stretch" Background="Transparent" 
                 Grid.Column="1" Grid.Row="2"
                 ItemsSource="{Binding ShoppingList}" 
                 SelectedItem="{Binding CurrentShoppingItem}" 
                 SelectedIndex="0">
            <ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Margin="6,0,0,0" Text="{Binding Name,  Converter={con:CaseConverter TargetCasing=Upper}}" FontSize="15"/>
                                <Border Height="1" BorderThickness="1" BorderBrush="Black" Margin="0,0,6,6"/>
                            </StackPanel>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListBox.GroupStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Width="200" Margin="0,0,3,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Column="0" TextBlock.FontSize="15" Text="{Binding caption}"/>
                        <TextBlock Grid.Column="1" TextBlock.FontSize="15" HorizontalAlignment="Right" Text="{Binding default_quantity}"/>
                        <TextBlock Grid.Column="0" Grid.Row="1" Text="{Binding category}"/>
                        <TextBlock Grid.Column="1" Grid.Row="1"  HorizontalAlignment="Right" Text="{Binding unit_size}"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <StackPanel Margin="6" Grid.Column="2" Grid.Row="2"  DataContext="{Binding CurrentShoppingItem}">
            <TextBlock Margin="0,0,0,0" Text="Name:"/>
            <TextBox Text="{Binding caption}" FontSize="15"/>
            <TextBlock Margin="0,6,0,0" Text="Category:"/>
            <TextBox Text="{Binding category}" FontSize="15"/>
            <TextBlock Margin="0,6,0,0" Text="Preferred Store:"/>
            <TextBox Text="{Binding preferred_shop}" FontSize="15"/>
            <TextBlock Margin="0,6,0,0" Text="Default Quantity:"/>
            <TextBox Text="{Binding default_quantity}" FontSize="15"/>
            <TextBlock Margin="0,6,0,0" Text="Unit Size:"/>
            <TextBox Text="{Binding unit_size}" FontSize="15"/>
        </StackPanel>
    </Grid>
</Window>
